class Solution:
    def appendCharacters(self, s: str, t: str) -> int:
        if t in s:
            return 0
        i = 0
        for char in s:
            if char == t[i]:
                i += 1

        return len(t) - i
