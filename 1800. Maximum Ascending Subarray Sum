class Solution:
    def maxAscendingSum(self, nums: List[int]) -> int:
        total = result= nums[0]
        for i in range(1, len(nums)):
            if nums[i]>nums[i-1]:
                total += nums[i]
                result = max(result, total)
            else:
                total = nums[i]
        return result
